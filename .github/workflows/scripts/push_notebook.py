import sys

if __name__ == "__main__":
    args = sys.argv[1:]
    print(
        f"TODO: Populate a .ipynb jinja template with path: {args[-1]}\n"
        "TODO: Execute notebook with nbcovert cli?"
        f"TODO: Push notebook to pangeo-forge/recipe-test-notebooks using {args[:-1]} "
        "for directory and filename args."
    )
